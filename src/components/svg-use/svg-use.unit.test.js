/* eslint-disable no-unused-expressions */
import { fixture, defineCE, assert, oneEvent, nextFrame } from '@open-wc/testing';
import SvgUse from '../src/svg-use';

describe('Unit | SvgUse', () => {
    it('can be created without errors', async () => {
        const el = new SvgUse();
    });
});
