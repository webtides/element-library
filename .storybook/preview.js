const html = String.raw;

/** @type { import('@storybook/web-components').Preview } */
const preview = {
    parameters: {
        options: {
            storySort: {},
        },
    },
    decorators: [],
};

export default preview;
